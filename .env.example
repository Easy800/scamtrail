# Copy to .env locally. Never commit real secrets.
# HMAC pepper and vault keys must not live in the same file as ordinary app secrets in production.

DATABASE_URL=postgres://scamtrail:scamtrail@127.0.0.1:5432/scamtrail
# APP_SECRET=
# VAULT_ENCRYPTION_KEY=
# HMAC_PEPPER=
# P2_UNWRAP_KEY=
